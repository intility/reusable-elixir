name: Build, test and push Elixir

on:
  workflow_call:
    inputs:
      example-input:
        description: "Descriptive description"
        type: string
        default: "value"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

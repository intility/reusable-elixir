name: Build and deploy Elixir docs

on:
  workflow_call:
    inputs:
      elixir-version:
        type: string
        required: false
        description: Elixir version (uses .tool-versions if not specified)
      otp-version:
        type: string
        required: false
        description: Erlang/OTP version (uses .tool-versions if not specified)
      hex-organization:
        type: string
        required: false
        description: Hex organization for private packages
      apt-packages:
        type: string
        required: false
        description: Space-separated list of APT packages to install (e.g., libvips-dev imagemagick)
      env:
        type: string
        required: false
        description: |
          Environment variables to set (one KEY=VALUE per line).
          Example:
            env: |
              CLOAK_KEY=base64encodedkey
      artifacts:
        type: string
        required: false
        description: |
          Multiline artifact definitions to download before compilation (one per line, format: name:path).
          Example:
            artifacts: |
              my-binary:apps/my_app/priv/bin
    secrets:
      hex-organization-key:
        required: false
        description: Hex organization authentication key
      ssh-private-key:
        required: false
        description: SSH private key(s) for accessing private Git repositories

jobs:
  build:
    name: Build Docs
    runs-on: ubuntu-latest
    steps:
      - uses: intility/reusable-elixir/.github/actions/set-env@2267ff22e747d316cf4548c8fa14e53be72a92f3 # v3.0.0
        if: ${{ inputs.env != '' }}
        with:
          env: ${{ inputs.env }}
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4
      - uses: intility/reusable-elixir/.github/actions/apt-packages@2267ff22e747d316cf4548c8fa14e53be72a92f3 # v3.0.0
        if: ${{ inputs.apt-packages != '' }}
        with:
          packages: ${{ inputs.apt-packages }}
      - uses: intility/reusable-elixir/.github/actions/setup-tool-versions@2267ff22e747d316cf4548c8fa14e53be72a92f3 # v3.0.0
        with:
          elixir-version: ${{ inputs.elixir-version }}
          otp-version: ${{ inputs.otp-version }}
      - uses: intility/reusable-elixir/.github/actions/ssh-agent@2267ff22e747d316cf4548c8fa14e53be72a92f3 # v3.0.0
        with:
          ssh-private-key: ${{ secrets.ssh-private-key }}
      - uses: intility/reusable-elixir/.github/actions/install-elixir@2267ff22e747d316cf4548c8fa14e53be72a92f3 # v3.0.0
      - uses: intility/reusable-elixir/.github/actions/mix-cache@2267ff22e747d316cf4548c8fa14e53be72a92f3 # v3.0.0
        with:
          mix-env: dev
      - uses: intility/reusable-elixir/.github/actions/download-artifacts@2267ff22e747d316cf4548c8fa14e53be72a92f3 # v3.0.0
        if: ${{ inputs.artifacts != '' }}
        with:
          artifacts: ${{ inputs.artifacts }}
      - name: Authorize Hex organization
        if: ${{ inputs.hex-organization != '' }}
        env:
          HEX_ORG: ${{ inputs.hex-organization }}
          HEX_ORG_KEY: ${{ secrets.hex-organization-key }}
        run: mix hex.organization auth "${HEX_ORG}" --key "${HEX_ORG_KEY}"
      - uses: intility/reusable-elixir/.github/actions/mix-deps-get@2267ff22e747d316cf4548c8fa14e53be72a92f3 # v3.0.0
      - uses: intility/reusable-elixir/.github/actions/mix-docs@2267ff22e747d316cf4548c8fa14e53be72a92f3 # v3.0.0
        with:
          mix-env: dev
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: doc/

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
